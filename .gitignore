# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/
*.tsbuildinfo

# Turbo
.turbo/

# Environment
.env
.env.local
.env.*.local
!.env.example

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Local file storage volume (case files, generated PDFs) — never commit
storage/cases/*
storage/invoices/*
storage/statements/*
!storage/cases/.gitkeep
!storage/invoices/.gitkeep
!storage/statements/.gitkeep

# Test / coverage
coverage/
.nyc_output/

# Scratch storage root written by the API end-to-end suite (see test/support/env.ts)
apps/api/test/.tmp-storage/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# LibreOffice/Word lock files, created while the DRS in the repo root is open
.~lock.*#

# Docker volume data
docker/data/

# Local userspace dev data (embedded Postgres)
.local/

# Design source handed over for the rebrand (brand guidelines, landing page
# mockup). Reference material rather than build input, and heavy — kept local.
/*.pdf

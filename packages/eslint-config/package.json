{
  "name": "@dental/eslint-config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared ESLint config for the Apex Digital Lab monorepo (TypeScript).",
  "main": "index.js",
  "scripts": {
    "lint": "echo \"config package — nothing to lint\" && exit 0",
    "typecheck": "echo \"no types\" && exit 0",
    "test": "echo \"no tests\" && exit 0",
    "build": "echo \"no build\" && exit 0"
  },
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint-config-prettier": "^9.1.0"
  }
}

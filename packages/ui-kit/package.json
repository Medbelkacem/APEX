{
  "name": "@dental/ui-kit",
  "version": "1.0.0",
  "private": true,
  "description": "Reusable UI components shared across Apex Digital Lab frontends (reserved for cross-app reuse).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"ui-kit — nothing to lint yet\" && exit 0",
    "typecheck": "echo \"ui-kit — no standalone typecheck yet\" && exit 0",
    "test": "echo \"no tests\" && exit 0",
    "build": "echo \"no build\" && exit 0"
  }
}

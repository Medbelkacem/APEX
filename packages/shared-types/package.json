{
  "name": "@dental/shared-types",
  "version": "1.0.0",
  "private": true,
  "description": "Types and enums shared between the Apex Digital Lab API and web app.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "lint": "echo \"types-only package — nothing to lint\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no tests\" && exit 0",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
